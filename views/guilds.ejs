<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>DuckBot | Guilds</title>
      <link rel="stylesheet" href="/global.css" />
      <link rel="stylesheet" href="/guilds.css" />
   </head>
   <body>
      <div class="title">Welcome back <span id="user-name"><%= user.global_name %></span>!</div>
      <br /><br />
      <div id="admin-guilds" class="guilds">
         <% for(let i = 0; i < adminGuilds.length; i++) { %>
         <a href="/guild/<%= adminGuilds[i].id %>" class="guild round-corners" id="g<%= adminGuilds[i].id %>">
            <img
               src="https://cdn.discordapp.com/icons/<%= adminGuilds[i].id %>/<%= adminGuilds[i].icon %>.png?size=128"
               onerror="document.querySelector('#g<%= adminGuilds[i].id %> img').style.background = '#575757'"
            />
            <span><%= adminGuilds[i].name %></span>
         </a>
         <% } %>
      </div>
   </body>
</html>
